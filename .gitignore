# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment file. Only PUBLIC client values (VITE_*) ever belong in a
# committed file (see .env.example). Real secrets live in the platform env, never here.
.env

# Working dir for CSVs pulled from Drive before import (scripts/import-drive-csv.mjs)
tmp/

# CodeGraph local index — rebuilt per machine, never committed
.codegraph/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
