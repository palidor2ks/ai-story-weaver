/**
 * Canonical brand host shown on shareable stat cards.
 *
 * Always use this constant rather than `window.location.host` so cards
 * display the marketing domain instead of whatever host happens to be
 * serving the page (e.g. preview/staging URLs).
 */
export const BRAND_HOST = 'www.polipulseapp.com';
